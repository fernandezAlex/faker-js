{
  "name": "faker",
  "version": "1.0.0",
  "description": "test with faker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch data.json --port 3000" 
  },
  "author": "Alex Fernandez",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.17.0"
  }
}
